@model InmobiliariaTPI.Models.Propietario

@{
    ViewData["Title"] = "Crear Propietario";
}

<h1>Crear Propietario</h1>

<hr />
<div class="row" id="app-propietario">
    <div class="col-md-6">
        <form asp-action="Create" method="post" @@submit.prevent="guardar">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group mb-3">
                <label asp-for="NombreCompleto" class="control-label"></label>
                <input asp-for="NombreCompleto" class="form-control" v-model="propietario.nombreCompleto" />
                <span class="text-danger" v-if="errorNombreCompleto">{{ errorNombreCompleto }}</span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="Dni" class="control-label"></label>
                <input asp-for="Dni" class="form-control" v-model="propietario.dni" />
                <span class="text-danger" v-if="errorDni">{{ errorDni }}</span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="Email" class="control-label"></label>
                <input asp-for="Email" class="form-control" type="email" v-model="propietario.email" />
                <span class="text-danger" v-if="errorEmail">{{ errorEmail }}</span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="Telefono" class="control-label"></label>
                <input asp-for="Telefono" class="form-control" v-model="propietario.telefono" />
                <span class="text-danger" v-if="errorTelefono">{{ errorTelefono }}</span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="Direccion" class="control-label"></label>
                <input asp-for="Direccion" class="form-control" v-model="propietario.direccion" />
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-primary" :disabled="guardando || !formularioValido">
                    <i class="bi bi-check-circle me-1"></i>
                    {{ guardando ? 'Creando...' : 'Crear' }}
                </button>
                <a asp-action="Index" class="btn btn-secondary">
                    <i class="bi bi-arrow-left me-1"></i>Volver
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    <script>
        const { createApp } = Vue;

        createApp({
            data() {
                return {
                    propietario: {
                        nombreCompleto: '',
                        dni: '',
                        email: '',
                        telefono: '',
                        direccion: ''
                    },
                    guardando: false
                }
            },
            computed: {
                errorNombreCompleto() {
                    if (!this.propietario.nombreCompleto) return '';
                    if (this.propietario.nombreCompleto.length > 150) return 'El nombre no puede superar los 150 caracteres.';
                    if (!/^[A-Za-zÁÉÍÓÚáéíóúÑñÜü\s']+$/.test(this.propietario.nombreCompleto)) {
                        return 'El nombre solo puede contener letras y espacios.';
                    }
                    return '';
                },
                errorDni() {
                    if (!this.propietario.dni) return '';
                    if (this.propietario.dni.length < 7 || this.propietario.dni.length > 20) {
                        return 'El DNI debe tener entre 7 y 20 caracteres.';
                    }
                    return '';
                },
                errorEmail() {
                    if (!this.propietario.email) return '';
                    if (!/^[^\s@@]+@@[^\s@@]+\.[^\s@@]+$/.test(this.propietario.email)) {
                        return 'El email no tiene un formato válido.';
                    }
                    return '';
                },
                errorTelefono() {
                    if (!this.propietario.telefono) return '';
                    if (!/^[0-9+\-\s()]+$/.test(this.propietario.telefono)) {
                        return 'El teléfono solo puede contener números, +, -, espacios y paréntesis.';
                    }
                    const soloDigitos = this.propietario.telefono.replace(/\D/g, '');
                    if (soloDigitos.length < 8) {
                        return 'El teléfono debe tener al menos 8 dígitos.';
                    }
                    if (soloDigitos.length > 15) {
                        return 'El teléfono no puede tener más de 15 dígitos.';
                    }
                    return '';
                },
                formularioValido() {
                    const telDigitos = this.propietario.telefono
                        ? this.propietario.telefono.replace(/\D/g, '').length
                        : 0;

                    return this.propietario.nombreCompleto &&
                           this.propietario.nombreCompleto.length <= 150 &&
                           /^[A-Za-zÁÉÍÓÚáéíóúÑñÜü\s']+$/.test(this.propietario.nombreCompleto) &&
                           this.propietario.dni &&
                           this.propietario.dni.length >= 7 &&
                           this.propietario.dni.length <= 20 &&
                           this.propietario.email &&
                           /^[^\s@@]+@@[^\s@@]+\.[^\s@@]+$/.test(this.propietario.email) &&
                           (!this.propietario.telefono ||
                               (/^[0-9+\-\s()]+$/.test(this.propietario.telefono) &&
                                telDigitos >= 8 &&
                                telDigitos <= 15));
                }
            },
            methods: {
                async guardar() {
                    if (!this.formularioValido) return;
                    this.guardando = true;
                    try {
                        const form = document.querySelector('form');
                        const formData = new FormData(form);
                        formData.set('NombreCompleto', this.propietario.nombreCompleto);
                        formData.set('Dni', this.propietario.dni);
                        formData.set('Email', this.propietario.email);
                        formData.set('Telefono', this.propietario.telefono);
                        formData.set('Direccion', this.propietario.direccion);

                        const response = await fetch(form.action, {
                            method: 'POST',
                            body: formData
                        });

                        if (response.redirected) {
                            window.location.href = response.url;
                        } else {
                            const html = await response.text();
                            document.documentElement.innerHTML = html;
                        }
                    } catch (error) {
                        alert('Error de conexión: ' + error.message);
                    } finally {
                        this.guardando = false;
                    }
                }
            }
        }).mount('#app-propietario');
    </script>
}